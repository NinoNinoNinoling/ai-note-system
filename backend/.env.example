# AI Note System - 환경변수 설정 (LangChain RAG 포함!)
# 이 파일을 .env로 복사해서 사용하세요

# Flask 기본 설정
SECRET_KEY=your-secret-key-for-assignment
FLASK_DEBUG=True

# 데이터베이스 (SQLite 기본값)
DATABASE_URL=sqlite:///ai_notes.db

# Claude API 키 (선택적 - 없어도 Mock 모드로 동작)
ANTHROPIC_API_KEY=sk-ant-your-claude-api-key-here

# 🔥 RAG 시스템 설정 (LangChain 과제용)
RAG_INDEX_PATH=data/note_vectors.index
RAG_METADATA_PATH=data/notes_metadata.json

# ========== 사용법 ==========
# 1. 이 파일을 .env로 복사: cp .env.example .env
# 2. ANTHROPIC_API_KEY에 실제 Claude API 키 입력 (선택적)
# 3. SECRET_KEY를 원하는 값으로 변경
# 4. RAG 패키지 설치: pip install faiss-cpu sentence-transformers
# 5. python run.py로 서버 실행

# ========== 주요 기능 ==========
# - LangChain RAG 아키텍처 구현
# - FAISS 벡터 검색 엔진
# - Claude AI와 연동
# - 노트 기반 지식 검색

# ========== 주의사항 ==========
# .env 파일은 Git에 커밋하지 마세요!
# Claude API 키가 없어도 Mock 모드로 테스트 가능합니다.
# RAG 기능은 faiss-cpu, sentence-transformers 설치 필요